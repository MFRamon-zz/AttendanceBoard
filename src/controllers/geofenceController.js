import firebase from "firebase/app";
import "firebase/database";

const updateGeofence = classroomName => {
  // firebase
  //   .database()
  //   .ref("classrooms/").child(0).on('value').
};

export { updateGeofence };
